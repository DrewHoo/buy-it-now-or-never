node_modules
dist
.DS_Store
.vite
*.log
.claude/

# Generated by `npm run fetch` (or implicitly by `npm run build`).
# Kept out of git so the repo doesn't bloat as the universe grows;
# CI regenerates these on every deploy.
public/data/
